import React from "react";
import Typography from "@mui/material/Typography";
import { Box } from "@mui/system";

export const UploaderRow = () => {
	return (
		<Box>
			<Typography variant="body1" color="initial">
				Uploader Row
			</Typography>
		</Box>
	);
};
