import React from "react";
import { Box, Typography } from "@mui/material";

const Uploader = (props) => {
	return (
		<Box>
			<Typography>Uploader Component</Typography>
		</Box>
	);
};

export { Uploader };
