import React from "react";
import { Box, Typography } from "@mui/material";

const SaleCode = (props) => {
	return (
		<Box>
			<Typography>SaleCode Component</Typography>
		</Box>
	);
};

export { SaleCode };
