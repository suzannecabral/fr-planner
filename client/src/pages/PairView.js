import React from "react";
import { Box, Typography } from "@mui/material";

const PairView = (props) => {
	return (
		<Box>
			<Typography>PairView Component</Typography>
		</Box>
	);
};

export { PairView };
