import React from "react";
import { Box, Typography } from "@mui/material";

const BioCode = (props) => {
	return (
		<Box>
			<Typography>BioCode Component</Typography>
		</Box>
	);
};

export { BioCode };
